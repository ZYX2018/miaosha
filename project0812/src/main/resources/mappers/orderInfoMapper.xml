<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="java0812.demo0812.miaosha.sqldao.orderInfoDao">

    <insert id="inserOneOrderInfo" parameterType="orderInfo" keyProperty="id" useGeneratedKeys="true">
INSERT INTO orderinfo (userId,goodsId,deliveryAddr,goodsName,goodsCount,goodsPrice,orderChannel,status,createDate)
VALUES(#{userId},#{goodsId},#{deliveryAddrId},#{goodsName},#{goodsCount}, #{goodsPrice},#{orderChannel},#{status},#{createDate})
</insert>

</mapper>